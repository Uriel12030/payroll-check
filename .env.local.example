# ============================================================
# Payroll Check – Environment Variables
# Copy this file to .env.local for local development.
# In production (Vercel), set these in:
#   Project Settings → Environment Variables
# ============================================================

# ------ Supabase (required) ---------------------------------
# Found in: Supabase Dashboard → Settings → API

# Your project URL (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anon / public key (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key — SERVER ONLY, never expose to the client!
# Used for privileged inserts that bypass Row Level Security.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ------ Admin access (required for admin UI) ----------------
# Comma-separated list of email addresses allowed to access /admin.
# If this variable is empty or missing, NO ONE can access admin.
# Example: admin@example.com,owner@company.com
ADMIN_ALLOWLIST_EMAILS=admin@example.com

# ------ Resend Email (required for admin email feature) ------
# Get these from https://resend.com/settings

# Resend API key for sending emails
RESEND_API_KEY=re_xxxxxxxxxxxx

# The "From" address for outbound emails (must be a verified domain in Resend)
EMAIL_FROM=support@yourdomain.com

# The domain used for inbound reply routing.
# Configure this in Resend Dashboard → Domains → Inbound.
# Reply-to addresses will be: reply+<token>@<this-domain>
EMAIL_INBOUND_DOMAIN=inbound.yourdomain.com

# Resend webhook signing secret (from Resend → Webhooks settings)
# Used to verify inbound webhook signatures (Svix).
RESEND_WEBHOOK_SECRET=whsec_xxxxxxxxxxxx

# ------ AI Email Assistant (required for AI analysis feature) --
# Get an API key from https://platform.openai.com/api-keys

# OpenAI API key for AI email analysis
OPENAI_API_KEY=sk-xxxxxxxxxxxx

# Model to use (default: gpt-4.1-mini). Other options: gpt-4.1, gpt-4o, etc.
AI_MODEL=gpt-4.1-mini
